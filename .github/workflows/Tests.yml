name: tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install Poetry
      run:
       pip install poetry

    - name: Install dependencies
      run:
        pip install pytest
        pip install pytest-cov
        pip install flake8

    - name: Install
      run:
        build
        package-install

    - name: Install dependencies
      run: make

    - name: Run check
      run: 
       lint

    - name: Run tests
      run:
       test
